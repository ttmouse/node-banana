# Folder: src/store/

> Trigger: Update when responsibilities or file list meaningfully change.
> 触发：当职责或关键文件列表发生"实质变化"时更新。

Zustand state management stores. / Zustand 状态管理存储。
Upstream: zustand, @xyflow/react; Downstream: all components. / 上游：zustand, @xyflow/react；下游：所有组件。
State persisted to localStorage; images cached in IndexedDB. / 状态持久化到 localStorage；图像缓存在 IndexedDB。

## Core Files
- `workflowStore.ts`: Main workflow state (nodes, edges, execution) / 主工作流状态
- `annotationStore.ts`: Annotation modal state / 标注模态框状态
