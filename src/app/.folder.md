# Folder: src/app/

> Trigger: Update when responsibilities or file list meaningfully change.
> 触发：当职责或关键文件列表发生"实质变化"时更新。

Next.js App Router entry point: routing, layout, API endpoints. / Next.js 应用路由入口：路由、布局、API端点。
Upstream: Next.js framework; Downstream: components/, store/. / 上游：Next.js框架；下游：组件与状态。
API routes handle server-side logic; pages are client-rendered. / API路由处理服务端逻辑，页面客户端渲染。

## Core Files
- `layout.tsx`: Root layout with providers / 根布局与全局Provider
- `page.tsx`: Main canvas page entry / 主画布页面入口
- `globals.css`: Global styles / 全局样式
- `api/generate/route.ts`: Image generation API (Gemini) / 图像生成API
- `api/llm/route.ts`: LLM text generation API / LLM文本生成API
