# Folder: src/utils/

> Trigger: Update when responsibilities or file list meaningfully change.
> 触发：当职责或关键文件列表发生"实质变化"时更新。

Utility functions and services. / 工具函数与服务。
Standalone utilities; consumed by store/ and components/. / 独立工具；被 store/ 和 components/ 消费。
Client-side only (IndexedDB, Canvas APIs). / 仅客户端（IndexedDB, Canvas API）。

## Core Files
- `imageCache.ts`: IndexedDB image persistence / IndexedDB 图像持久化
- `gridSplitter.ts`: Image grid splitting utility / 图像网格分割工具
